# UX Audit — CAS Incubator OS
**Date**: 2026-04-25  
**Audited by**: Claude (automated + manual review)  
**Commit at audit time**: 0c1f896  
**Fixed in commit**: a4fc3fe

---

## Summary

Audited all role-based routes (super_admin, teacher, mentor, student, parent), every sidebar nav link, every `<Link href=` and `<a href=` in the codebase, and common UX patterns (empty states, forms, shells).

**Total issues found**: 23  
**Fixed this pass (🔴 critical + top UX)**: 12  
**Left for next pass**: 11

---

## Issues Found

### 🔴 BROKEN (404 from in-app link)

| # | Severity | Route / Component | Issue | Fix Applied |
|---|----------|-------------------|-------|-------------|
| 1 | 🔴 | Admin sidebar → `/admin/cohorts` | Sidebar link exists in `components/admin/Shell.tsx:38` but `/admin/cohorts/page.tsx` was missing entirely — every admin clicking "Cohorts" got a 404 | **FIXED**: Created `/app/admin/cohorts/page.tsx` with full table listing all cohorts |
| 2 | 🔴 | `app/teacher/rubrics/page.tsx:24` | "New Rubric" button links to `/admin/rubrics/new` — teachers are redirected away from their shell into admin territory | **FIXED**: Removed cross-role link; added note that rubric creation is admin-managed |
| 3 | 🔴 | `app/teacher/rubrics/page.tsx:44` | "Edit" link per rubric goes to `/admin/rubrics/${r.id}` — same cross-role issue | **FIXED**: Removed edit links; teachers now have read-only view |

---

### 🟡 INCOMPLETE (page renders but inside wrong shell / no nav)

| # | Severity | Route / Component | Issue | Fix Applied |
|---|----------|-------------------|-------|-------------|
| 4 | 🟡 | `app/teacher/parents/page.tsx:5` | Imports `@/components/admin/Shell` instead of `@/components/teacher/Shell` — teacher sees admin sidebar on parent comms page | **FIXED** |
| 5 | 🟡 | `app/teacher/showcases/page.tsx` | Uses bare `div.min-h-screen` instead of teacher Shell — no sidebar/nav | **FIXED** |
| 6 | 🟡 | `app/teacher/risks/page.tsx` | Uses bare `div.min-h-screen` instead of teacher Shell — no sidebar/nav | **FIXED** |
| 7 | 🟡 | `app/mentor/showcases/page.tsx` | Uses bare `div.min-h-screen` instead of mentor Shell — no sidebar/nav | **FIXED** |
| 8 | 🟡 | `app/mentor/parents/page.tsx` | Uses bare `div.min-h-screen` instead of mentor Shell — no sidebar/nav | **FIXED** |
| 9 | 🟡 | `app/mentor/risks/page.tsx` | Uses bare `div.min-h-screen` instead of mentor Shell — no sidebar/nav | **FIXED** |
| 10 | 🟡 | Teacher Shell NAV | `/teacher/parents`, `/teacher/showcases`, `/teacher/risks` all exist as pages but were NOT in the sidebar nav — unreachable via UI | **FIXED**: Added all 3 to teacher sidebar |
| 11 | 🟡 | Mentor Shell NAV | `/mentor/parents`, `/mentor/showcases`, `/mentor/risks` all exist but were NOT in the sidebar nav | **FIXED**: Added all 3 to mentor sidebar |

---

### 🟡 INCOMPLETE (UX/form issues)

| # | Severity | Route / Component | Issue | Fix Applied |
|---|----------|-------------------|-------|-------------|
| 12 | 🟡 | `app/admin/cohorts/new/page.tsx:62` | Program ID field has `placeholder="UUID of program"` — no guidance on where to find the UUID | **FIXED**: Added hint text linking to Programs page with instructions |
| 13 | 🟡 | `app/not-found.tsx` | Default 404 showed generic "Page not found" with only a home link — signed-in users had no role-aware navigation | **FIXED**: Role-aware 404 with dashboard link based on `getCurrentUser().role` |

---

### 🟢 MINOR (not fixed this pass)

| # | Severity | Route / Component | Issue | Suggested Fix |
|---|----------|-------------------|-------|---------------|
| 14 | 🟢 | `app/page.tsx` | Footer links to `/docs/LEGAL/privacy.md` and `/docs/LEGAL/terms.md` — these are `.md` files served as-is, not rendered pages | Create `/app/legal/privacy/page.tsx` and `/app/legal/terms/page.tsx` rendering the markdown |
| 15 | 🟢 | `app/teacher/page.tsx:62` | Dashboard shows `{t("studentsToCheck")}: {t("placeholder")}` — placeholder text visible to teachers | Wire actual count from DB or remove until implemented |
| 16 | 🟢 | `app/mentor/page.tsx:50` | "Projects Awaiting" section shows `{t("placeholder")}` — no real data | Wire actual pending count from `projects` where `stage_status = submitted` |
| 17 | 🟢 | `app/student/page.tsx:79` | Current stage hardcoded as `"Stage 1 — Interest Discovery"` — not read from DB | Read from `student_method_progress` |
| 18 | 🟢 | `app/student/page.tsx:82` | Next task shows `{t("nextTaskPlaceholder")}` — always empty | Wire from worksheet assignments |
| 19 | 🟢 | `app/admin/cohorts/new/page.tsx` | Program ID is a free-text UUID field — extremely UX-unfriendly for a required FK. `router.back()` cancel button can lose context | Replace with dropdown of programs fetched server-side; change cancel to go to `/admin/cohorts` |
| 20 | 🟢 | Teacher/mentor dashboards | Dashboard index pages (`/teacher`, `/mentor`) use a full-width centered layout without the Shell sidebar — inconsistent with all other teacher/mentor pages | Wrap teacher/mentor dashboards in their respective Shell components |
| 21 | 🟢 | Student role | No sidebar navigation — student jumps between pages via inline `<Link>` scattered through content. Hard to discover `/student/checkpoints`, `/student/resources`, `/student/worksheets` | Add a simple student nav bar with links |
| 22 | 🟢 | Parent role | No sidebar or nav links. `/parent/updates` is reachable from dashboard but not listed anywhere. No link back from `/parent/updates/[id]` to parent dashboard | Add nav bar or at least breadcrumb |
| 23 | 🟢 | `app/admin/cohorts/new/page.tsx` | Date inputs have no timezone hint (e.g., "Dates are in UTC") — ambiguous for international cohorts | Add `(UTC)` label to date fields |

---

## Per-Role Summary

### Admin (super_admin)
**Dead links**: `/admin/cohorts` (index) — **FIXED**  
**Sidebar coverage**: All 19 nav items now have working pages  
**Remaining**: Cohort new-form UX (UUID input), legal page links

### Teacher
**Dead links**: 0 (all href targets exist)  
**Shell issues**: parents/showcases/risks used wrong/no shell — **FIXED**  
**Nav gaps**: parents/showcases/risks were hidden from sidebar — **FIXED**  
**Cross-role links**: rubrics edit/create → admin — **FIXED**  
**Remaining**: Dashboard not in Shell, placeholder text, teacher/students not in sidebar

### Mentor
**Dead links**: 0  
**Shell issues**: showcases/parents/risks used no shell — **FIXED**  
**Nav gaps**: parents/showcases/risks not in sidebar — **FIXED**  
**Remaining**: Dashboard not in Shell, placeholder text

### Student
**Dead links**: 0  
**Remaining**: No sidebar nav, hardcoded stage data, placeholder next task text

### Parent
**Dead links**: 0  
**Remaining**: No nav bar, no breadcrumb from update detail, no link to profile settings

---

## Top 10 Issues Found (All Severities)

1. 🔴 `/admin/cohorts` — 404 on admin sidebar click (admin Shell NAV line 38)
2. 🔴 `teacher/rubrics/page.tsx:24,44` — cross-role links to `/admin/rubrics/*`
3. 🟡 `teacher/parents/page.tsx:5` — imports wrong Shell (admin instead of teacher)
4. 🟡 `teacher/showcases/page.tsx` — no Shell (bare layout)
5. 🟡 `teacher/risks/page.tsx` — no Shell (bare layout)
6. 🟡 `mentor/showcases/page.tsx` — no Shell (bare layout)
7. 🟡 `mentor/parents/page.tsx` — no Shell (bare layout)
8. 🟡 `mentor/risks/page.tsx` — no Shell (bare layout)
9. 🟡 Teacher sidebar missing: parents, showcases, risks links
10. 🟡 Mentor sidebar missing: parents, showcases, risks links

## Top 5 Fixed This Pass

1. **Created `/admin/cohorts/page.tsx`** — admin sidebar "Cohorts" no longer 404s
2. **Teacher/mentor Shell consistency** — 6 pages (teacher/parents, teacher/showcases, teacher/risks, mentor/showcases, mentor/parents, mentor/risks) now properly use their role Shell
3. **Teacher + Mentor sidebars** — 6 missing nav items added (parents, showcases, risks for each role)
4. **Teacher rubrics cross-role links** — removed `/admin/rubrics/new` and `/admin/rubrics/${id}` links from teacher view
5. **Role-aware 404** — `not-found.tsx` now detects user role via `getCurrentUser()` and shows role-appropriate dashboard link

## Top 10 Issues NOT Fixed (Next Priority)

1. `app/page.tsx` footer links to `.md` files — create proper legal pages
2. Teacher/mentor dashboard pages not wrapped in Shell — inconsistent UX
3. Student has no sidebar/nav — hard to discover features
4. Parent has no nav bar — updates link is the only way to navigate
5. `app/admin/cohorts/new/page.tsx` — program ID should be a dropdown
6. `app/teacher/page.tsx:62` — placeholder text visible in production
7. `app/mentor/page.tsx:50` — placeholder text visible in production
8. `app/student/page.tsx:79,82` — hardcoded stage + placeholder next task
9. Timestamps throughout app lack timezone context (e.g., `created_at.slice(0, 10)` without UTC label)
10. Teacher/students `[student_user_id]` page exists but is not linked from teacher sidebar

---

## Files Changed

| File | Change |
|------|--------|
| `app/not-found.tsx` | Role-aware 404 with dashboard link |
| `components/shared/ComingSoon.tsx` | New: reusable in-shell placeholder |
| `app/admin/cohorts/page.tsx` | New: cohorts index page |
| `app/admin/cohorts/new/page.tsx` | Improved program ID hint text |
| `components/teacher/Shell.tsx` | Added parents, showcases, risks to NAV |
| `components/mentor/Shell.tsx` | Added parents, showcases, risks to NAV |
| `app/teacher/parents/page.tsx` | Fixed Shell import (admin → teacher) |
| `app/teacher/showcases/page.tsx` | Wrapped in teacher Shell |
| `app/teacher/risks/page.tsx` | Wrapped in teacher Shell |
| `app/teacher/rubrics/page.tsx` | Removed cross-role admin links |
| `app/mentor/parents/page.tsx` | Wrapped in mentor Shell |
| `app/mentor/showcases/page.tsx` | Wrapped in mentor Shell |
| `app/mentor/risks/page.tsx` | Wrapped in mentor Shell |
